import React from 'react'

const Wrapper80 = (props) => {
    return (
        <div style={{width: '80%', margin: '0 auto'}}>
            {props.children}
        </div>
    )
}

export default Wrapper80